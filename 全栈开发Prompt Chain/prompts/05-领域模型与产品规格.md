# M05：领域模型与产品规格

## 用途

把已批准策略转成共享语言和可实现、可验收的产品规格。默认只写本地规格，不发布 tracker。

推荐 Skill：`domain-modeling`、`specification`；目标仓库 `to-spec` 只借鉴结构，不采用自动发布与标签行为。

## 输入变量

```text
{{A01_PATH}}
{{A03_PATH}}
{{A04_PATH}}
{{EXISTING_DOMAIN_DOCS}}
{{SPEC_OUTPUT}} = A05-product-spec.md
{{MODE}} = PLAN
```

## 可复制 Prompt

```text
你正在执行 M05「领域模型与产品规格」。本阶段不实现代码、不创建 Issue。

输入：
- A01_PATH: {{A01_PATH}}
- A03_PATH: {{A03_PATH}}
- A04_PATH: {{A04_PATH}}
- EXISTING_DOMAIN_DOCS: {{EXISTING_DOMAIN_DOCS}}
- SPEC_OUTPUT: {{SPEC_OUTPUT}}

前置门：A04 必须处于 approved，或用户明确允许生成 draft spec。否则停止并列出缺失决定。

方法：
1. 若 domain-modeling/specification 已安装且用户显式请求相应产物，加载它们。先读取现有 CONTEXT、ADR、schema、接口和术语，不重复创建第二套词汇。
2. 将 Observation、Interpretation、Assumption、Decision 分开。识别 entities、value objects、states、invariants、commands、events、actors、权限边界和失败语义。
3. 用具体场景检验词义：正常路径、边界、失败、并发/重复请求、取消/恢复、权限不足和数据不完整。
4. 只有同时满足“重大、存在真实备选、长期难逆”时才建议 ADR；普通实现细节不写 ADR。
5. Spec 描述可观察行为，不写逐文件施工清单。每项 requirement 都必须对应 acceptance；每项 acceptance 必须追溯到 requirement 或明确风险。
6. User journey 按优先级拆分，并保证每个高优先级 journey 可以独立验证。Acceptance 优先用 Given/When/Then 或同等可执行场景。
7. 若规格包含 UI，记录界面类型、目标 viewport/输入方式、信息密度、使用频率、主操作、次要操作和高风险操作；把相对显著性、图标可识别性、静止态层级和状态反馈写成可观察 acceptance。不得写跨场景统一像素值、指定新图标库，或把实现风格伪装成需求。
8. 明确 loading/empty/error/permission/timeout/retry/partial success 等状态；适用时覆盖数据生命周期、AI 输出不确定性、人工接管和滥用场景。
9. 定义 public test seams，但若新增 seam 会实质改变架构，则标记到 G3，不替用户决定。
10. 未决项不可用“业界最佳实践”猜测填平；它们保持 Open question 并阻塞相应 requirement。
11. 默认只写本地 A05。真实 tracker、label 或外部发布必须另行给出预览、幂等策略和授权请求。

输出 `A05-product-spec.md`：

# A05 Product Spec
## Metadata
## Facts
## Decisions
## Assumptions
## Risks and reversibility
## Acceptance evidence
## Problem and desired outcome
## Actors and permissions
## Ubiquitous language
| Term | Definition | Example | Non-example | Source |

## Domain model
- Entities/value objects
- States and transitions
- Invariants
- Commands/events

## User journeys
按 P0/P1/P2，逐项写前置、主流程、失败/恢复。

## UI context and visual acceptance
若包含 UI，记录界面类型、目标 viewport/输入方式、信息密度、使用频率、主/次要及高风险操作；以可观察 acceptance 描述相对显著性、图标可识别性、静止态层级和状态反馈，不规定跨场景统一像素或实现风格。

## Functional requirements
使用稳定 ID：FR-001...

## Non-functional and risk requirements
安全、隐私、性能、可访问性、兼容、可观测、AI Eval。

## Acceptance scenarios
使用 AC-001...，并写 `Traces to: FR-xxx/RISK-xxx`。

## Data and interface contracts
## Failure behavior
## Migration/compatibility considerations
## Out of scope
## Open questions
## Requirement-to-acceptance matrix
## Proposed test seams
## G3 decisions required
## Handoff

完成标准：实现者无需猜产品行为；每个 P0 行为可验收；词汇无冲突；未决架构/数据/权限选择未被伪装成已批准需求。
```
