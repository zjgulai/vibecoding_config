interface:
  display_name: "Implementation Orchestration"
  short_description: "Coordinate confirmed work in verified batches"
  default_prompt: "Use $implementation-orchestration to organize this confirmed spec or ticket set into small, verified implementation batches and report any plan deviation."
policy:
  allow_implicit_invocation: false
