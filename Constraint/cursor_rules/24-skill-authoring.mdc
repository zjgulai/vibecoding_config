---
description: 创建/修改 Agent Skills（Claude Code、Cursor、Kimi 等）时的强制规范，杜绝外置依赖、规范缩水与触发/术语不一致
globs:
  - "**/SKILL.md"
  - "**/.claude/skills/**"
  - "**/.cursor/skills/**"
  - "**/.kimi/skills/**"
  - "**/.config/agents/skills/**"
alwaysApply: true
---

# Agent Skill 编写约束

在用户要求**新建、迁移或大改**某 skill（含多工具各一份 `SKILL.md`）时，**必须**遵守以下条款；与 `code-change-minimalism` 不冲突时，本文件对 **SKILL 内容**优先。

## 1. 自包含（杜绝外置主依赖）

- `SKILL.md` **正文**须包含可独立执行的全套规范，**不得**以「见某某路径下的 `.md`」「与某教程对齐」作为**主执行依据**；若从外部文档迁出，应把**必要条文**整段或等价无歧义地写入 skill，而非留链接/文件名当步骤。
- 若仅能提供「资源链接」作补充，须标为**可选阅读**，并写明 **不读也能完成** 的闭环路径。

## 2. 禁止为「变短」而缩水

- 对**量表、编号列表、逐步输出格式、判据边界**（如 #i 与 #j 的区分、何种情况不算证据），**禁止**在「优化/精简」时压成一条概括而**删掉**可操作的完整句；若多平台有差异，采用 **「通用块 + 各端 Delta」**，通用块**不得**因 Delta 而缺失。
- 所谓「与某某版同一量表」的**指代**不能替代在**本文件内**的完整重述，除非同仓库内该 skill 已自带 `references/xxx` 且本 SKILL 有显式**按需读取**且评估器认可——默认仍不依赖。

## 3. 判定与证据规则成文、可执行

- 凡涉及多档状态（如 `+` / `~` / `-`）与**引语、证据、反例**的关系，**必须**在正文写清；避免出现「只规定 `[+]` 要引语而 `[~]` 未写」类歧义。默认原则：**无合格证据则打低档或最严档**，并在文中写明。

## 4. 术语与固定文案一致

- 产品或技能内**固定**的叫法、三态/四态标签、章节名，在**同 skill 多段落、多平台副本**间必须一致，**禁止**同一含义两种简称混用（例如状态标签在 UI 与正文不可一处省字、一处全写致使模型理解偏差）。
- 多语言/多产品副本时，在各自 Delta 中说明与通用块的**对译关系**即可，不得悄悄改名。

## 5. Frontmatter 与可发现性

- `name` 符合各平台命名限制；`description` 用**第三人称**写清**能力 + 何时应加载**（含用户**未明说**但意图接近的**近义场景**、弱提示词），减少漏触发；可参考「描述略长但覆盖全」的写法，**禁止**仅一句话糊任务。
- 对多工具部署的同一 skill，建议统一 **`version`** 或在文首**一行**注明同步关系，避免「只改了一端」的静默分叉。

## 6. 与自动化/Hook 的边界

- **Skill** 管「模型在对话里该遵循什么」；**落盘/定时/比例阈值** 等**可靠自动化**用 hooks、脚本或产品配置，**不要**在 SKILL 里写「模型应每 50% 自动存盘」而实际无法由模型保证——须拆成**可验**的机制 + skill 可只描述人工/协助步骤。

## 7. 交付前自查（对 Agent 的强制项）

在将 skill 交用户**定稿/安装路径**前，在对话中**显式过一遍**（可一句 checklist）至少：

- [ ] 不依赖**可移动/可删**外置单文件作为唯一真源。  
- [ ] 量表/步骤/输出模板**无因精简而缺项**。  
- [ ] 证据/档位规则**无歧义**。  
- [ ] 固定术语、标签**全篇一致**（含多副本）。  
- [ ] `description` **足以**触发、且含关键近义词。  
- [ ] 若存在多平台变体，**差异块**已写清且**通用块**未省。

## 8. 与 RIPER / 实现流程

- 若用户未说「执行」，对**仓库/用户目录**的写入仍遵循 `mode-gating` 与 `code-change-minimalism`；本文件不授权扩大改动范围。  
- 对 **SKILL 正文** 的补全/改写，不视为与「无关业务代码」的顺手重构。
