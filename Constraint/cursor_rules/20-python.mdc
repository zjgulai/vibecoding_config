
---

### **20-python.mdc**

```md
---
description: Python 后端开发规则
globs:
  - "**/*.py"
alwaysApply: true
---

# Python 开发规则

## 技术偏好

- Python 3.12+ 优先
- FastAPI > Django > Flask
- 优先 async/await
- PostgreSQL 优先
- SQLAlchemy 2.0 ORM
- Pydantic V2 优先
- pytest
- ruff
- mypy 严格模式
- uv 作为包管理工具

---

## 编码规则

- 默认补全类型注解
- 函数、方法、类接口必须清晰表达输入输出
- 优先使用标准库，再考虑第三方依赖
- 优先组合而不是无意义层级抽象
- 避免过度设计

---

## 异常与日志

- 不静默吞异常
- 异常处理必须可追踪
- 使用结构化日志
- 不要为了“看起来稳妥”添加无意义兜底逻辑

---

## 性能与实现

- 性能关键路径先分析，再优化
- 优先使用 cProfile 或等价工具定位瓶颈
- 不做基于猜测的优化
- 避免过早抽象

---

## 文件与目录规则

- 新建 Python 文件前，先判断是否应并入已有模块
- src/ 中禁止放临时测试文件、草稿脚本、一次性分析脚本
- 草稿 Python 脚本进入 drafts/scripts/
- 一次性脚本或调试脚本进入 tmp/ 或 archive/，不要污染正式区

---

## 测试规则

- 使用 pytest
- 覆盖关键路径和边界情况
- 不只验证 happy path
- 测试文件进入 tests/
