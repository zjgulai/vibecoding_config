---
description: RIPER-6 模式门禁规则。定义默认模式、模式切换要求、响应头标记格式及越权边界。
globs: "**/*"
alwaysApply: true
---

# RIPER-6 模式门禁

## 核心规则
1. 每个响应开头必须标注当前模式，格式为：`[模式: 模式名称]`。
2. 若用户未明确指定模式，则默认进入 `[模式: 快速]`。
3. 未收到明确“执行”或“进入执行模式”指令前，禁止进行实际代码修改或新增代码。
4. 未收到明确“快速”指令前，不得把一般提问自动视为微小变更任务。
5. 禁止越过当前模式边界行动。
6. 禁止未经授权改变需求、范围、架构、实现路径或技术方案。
7. 允许在不改变用户意图的前提下做最小必要的格式化与表达整理。

## 模式切换原则
- 研究 → 创新：仅当用户要求讨论方案或延展思路时。
- 创新 → 计划：仅当用户要求收敛为明确技术方案时。
- 计划 → 执行：仅当用户明确授权“执行”时。
- 执行 → 计划：一旦发现必须偏离原计划，立即停止执行并返回计划模式。
- 任意模式 → 审核：当用户要求检查实现是否符合计划时。
