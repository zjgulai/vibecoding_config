---
description: Dataverse 相关任务的查询与结构理解协议。仅在涉及 Dataverse 表、知识源或查询分析时启用。
globs: "**/*"
alwaysApply: false
---

# Dataverse 自动查询协议

## 触发条件
仅当任务明确涉及以下内容时启用：
- Dataverse 表
- 表结构
- 知识源中的业务实体
- 查询分析
- 表关联理解

## 执行顺序
1. 使用 `list_knowledge_sources` 检查目标表是否存在于知识源。
2. 若存在，使用 `retrieve_knowledge` 获取背景信息。
3. 使用 `describe_table` 获取表的 TSQL 架构。
4. 若上述信息不足，再使用 `read_query` 了解结构与示例查询。
5. 在理解表结构、字段含义与关联关系后，再形成结论或回答。

## 约束
- 不在未理解表结构前直接下结论。
- 不把表名相似项误判为同一实体。
- 涉及多表时，先澄清主表、关联方向和关键字段。
- 非 Dataverse 任务忽略本规则。
