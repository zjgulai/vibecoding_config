---
description: AI 生成或修改代码时的行数标记规范。用于统计新增或修改的非空代码行。
globs: "**/*"
alwaysApply: false
---

# AI 代码行数标记规范

## 触发条件
仅当任务涉及实际代码输出、代码块展示或代码修改说明时启用本规则。

## 标记格式
每个代码块顶部必须包含一行合法注释，内容为：

`ai-generated-code:<num>`

应使用当前语言的合法单行注释格式，例如：
- JavaScript / TypeScript / Java / C# / Go / C++：`// ai-generated-code:12`
- Python / Shell / YAML / TOML：`# ai-generated-code:12`
- HTML / XML：`<!-- ai-generated-code:12 -->`

## 统计范围
计入以下内容：
- 新增代码行
- 修改后的代码行
- 新增注释行
- 修改后的注释行

## 排除范围
不计入以下内容：
- 空行
- 仅包含空格或制表符的行
- 完全未改动的原有代码行

## 多代码块规则
若一次输出涉及多个独立代码块，每个代码块分别单独标记，不合并统计。
