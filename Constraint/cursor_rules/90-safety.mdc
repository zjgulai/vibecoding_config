---
description: 修改安全、备份、回退与禁止事项
globs:
  - "**/*"
alwaysApply: true
---

# 安全与操作规则

## 破坏性操作规则

以下行为前必须确认：

- 删除文件
- 批量移动文件
- 大范围重构
- 大量覆盖
- 删除目录
- 不可逆结构调整

高风险命令包括但不限于：

- rm -rf
- git reset --hard
- git clean -f

---

## 备份与回退

- 每次编辑文件前，先保留可回退路径
- 覆盖已有文件前，检查是否需要保留旧版本
- 必要时将旧版本转入 archive/ 或 drafts/
- 不在没有备份的情况下执行高风险操作

---

## 不做未请求的顺手修改

不要借执行一个任务之机，顺手加入用户未要求的：

- 清理
- 重命名
- 重构
- 配置补充
- 结构调整

---

## 禁止事项

禁止以下行为：

- 假设用户已经清楚需求
- 不指出更优路径而迎合次优方案
- 用补丁替代根因修复
- 输出不改变决策的信息
- 添加未请求的改进、清理、配置项
- 添加不必要的错误处理
- 为了兼容保留无用代码
- 在没有备份时执行破坏性操作
- 静默覆盖已有文件
- 把草稿放入正式目录
- 把临时产物放入正式目录
- 把归档和活跃内容混放
- 在根目录堆放杂项文件
- 使用 final_v2 一类命名制造结构噪音
